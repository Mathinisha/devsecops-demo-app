apiVersion: v1
kind: Service
metadata:
  name: devsecops-demo-app
spec:
  type: LoadBalancer
  selector:
    app: devsecops-demo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
